# Obsidian AI Web Clipper 運用手順書

## 1. 概要 (Overview)
**Obsidian AI Web Clipper**は、閲覧中のWebページの内容をAI（LLM）が自動で要約し、ObsidianへMarkdown形式でワンクリック保存するChrome拡張機能です。

---

## 2. インストール手順 (Installation)

本拡張機能は現在、Chromeのデベロッパーモードを利用して手動でインストールする必要があります。

1. **Chromeを起動**し、アドレスバーに以下を入力して Enter を押します。
   ```text
   chrome://extensions/
   ```
2. 画面右上にある **「デベロッパー モード」** のトグルスイッチを **ON（青色）** にします。
3. 画面左上に表示される **「パッケージ化されていない拡張機能を読み込む」** ボタンをクリックします。
4. ファイル選択画面が開くので、ダウンロードまたは作成した **`Clipper` フォルダ自体を選択** します。
5. 拡張機能一覧に **「Obsidian AI Web Clipper」** が追加されればインストール完了です。

> 💡 **おすすめの設定:**
> Chrome右上のジグソーパズルマーク（拡張機能アイコン）をクリックし、Obsidian Clipperの横にある **画びょうマーク（ピン留め）** を押しておくと、ツールバーからいつでもワンクリックで呼び出せるようになります。

---

## 3. 初期設定 (Configuration)

拡張機能を利用する前に、AIのAPIキーと保存先の設定を行います。

1. ピン留めした **Obsidian Clipper のアイコン** をクリックしてポップアップ画面を開きます。
2. **「⚙️ 設定」** をクリックして設定項目を展開します。
3. 以下の項目を入力します。

| 設定項目 | 説明 | 入力例 |
| :--- | :--- | :--- |
| **API Key** | AI要約に使用する任意のAPIキーを設定します。（例：OpenAI、OpenRouterなどご自身が利用したいサービスのAPIキーを入力してください） | `sk-...` |
| **Vault Name** | 保存したい **Obsidianの保管庫（Vault）の名前** を入力します。大文字・小文字も正確に入力してください。 | `MyVault` |
| **Folder Path** | 保管庫内の保存先フォルダのパスです。空欄の場合はVaultの直下に保存されます。 | `Clippings` |

4. **「設定を保存」** ボタンをクリックします。

> ⚠️ **ObsidianのVault名の確認方法:**
> Obsidianアプリを開き、左下の「保管庫を開く（フォルダアイコン）」をクリックすると、現在開いている保管庫の正確な名前が確認できます。

---

## 4. 実際の使い方 (Usage)

1. **Obsidianアプリを起動** しておきます（バックグラウンドで開いておくとスムーズに連携できます）。
2. Chromeで **要約・保存したいWebページ** を開きます。
3. Chrome右上の **Obsidian Clipper アイコン** をクリックします。
4. **「AI要約してObsidianへ保存」** ボタンをクリックします。
5. ポップアップ内のステータスが順番に切り替わります（ページ情報抽出 → AI要約生成 → Obsidian送信）。
6. 「✅ Obsidianへ保存しました！」と表示されると、自動的にObsidianアプリが開き、指定したフォルダに `YYYY-MM-DD_タイトル.md` というノートが新規作成されます。

---

## 5. よくあるトラブルと解決策 (Troubleshooting)

### Q1. 「chrome:// のページでは実行できません」というエラーが出る
- **原因:** Chromeの設定画面や新しいタブでは拡張機能が動作しません。
- **対処法:** 通常のWebページ（`https://...` で始まるページ）を開いた状態で実行してください。

### Q2. 「APIリクエストに失敗しました」というエラーが出る
- **原因:** APIキーが間違っている、またはご利用のAPIアカウントのクレジット（残高）が不足している可能性があります。
- **対処法:** 設定画面でAPIキーを再確認し、ご利用のAPIサービス上でクレジット残高があるか確認してください。

### Q3. 「Obsidianを開きますか？」という確認ポップアップが出る
- **原因:** ブラウザが外部アプリ（Obsidian）を起動するための安全確認です。
- **対処法:** **「常に許可する」** チェックボックスをオンにしてから開くようにすると、次回以降は自動で開くようになります。

### Q4. Obsidianに保存されない・アプリが反応しない
- **原因:** 設定した `Vault Name` が実際の保管庫名と一致していない可能性があります。
- **対処法:** 拡張機能の設定画面を開き、Vault名に余計なスペースが入っていないか、大文字・小文字が正確に合っているか再確認してください。
